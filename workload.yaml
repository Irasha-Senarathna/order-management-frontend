endpoints:
  http:
    type: HTTP
    port: 80
    targetPort: 3000
    visibility:
      - external
dependencies:
  endpoints:
    - component: order-backend
      name: http
      visibility: project
      envBindings:
        address: VITE_BACKEND_URL